# Python-Project---European-Airports-Data-Analyze
In this Python program, airport departure data was analyzed from CSV files. It validates user inputs, calculates flight statistics, saves results to a file, generates histograms, and allows analyzing multiple datasets.
